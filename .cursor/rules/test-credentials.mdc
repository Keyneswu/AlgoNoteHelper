---
description: Production/test login credentials for AlgoNoteHelper screenshots and manual QA
alwaysApply: true
---

# Test credentials

Use these when you need to log into the live app or a shared test account (screenshots, UI smoke checks, BFF calls). Do **not** put them in committed README body text or public docs beyond this rule.

| Item | Value |
|---|---|
| App | https://algonote.keyneswu.com |
| Email | `test@example.com` |
| Password | `test123` |
| Role | `user` (non-admin) |

Notes:

- Prefer this account for automated screenshots and non-destructive UI checks.
- Admin account is separate (`keyneswu2@gmail.com`) — do not guess or rotate its password unless the user asks.
- Public signup is disabled; do not try to register new users via `/login`.
