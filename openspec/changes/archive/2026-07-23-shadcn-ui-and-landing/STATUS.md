# STATUS: superseded / abandoned

This change is **not** being merged.

It is superseded by `openspec/changes/landing-login-polish`, which keeps HeroUI for the app UI and only adds a marketing landing plus polished login/setup.

Any local branch named `feature/shadcn-ui-and-landing` should remain unmerged.
